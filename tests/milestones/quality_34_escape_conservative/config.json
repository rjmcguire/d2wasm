{
  "name": "escape_conservative",
  "description": "Escape analysis is conservative — escaping pointers stay heap-allocated and work correctly",
  "type": "wasm_exec",
  "entry": "_D4test4mainFZi",
  "compiler_args": [
    "--escape-analysis"
  ],
  "capability": "escape analysis correctness — conservatively keeps escaping allocations on heap",
  "mangled_entry": "_D4test4mainFZi"
}